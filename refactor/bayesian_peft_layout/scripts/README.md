# Scripts

This folder is intentionally lightweight. The runnable Python entrypoint lives
in `run/main.py`, and shell examples live in:

- `run_eval_examples.sh`
- `run_benchmark_suite_example.sh`

The unified CLI now covers:

- MAP training launcher
- MAP
- MC-Dropout
- deep ensemble
- probability ensemble
- Seq-LoRA constant-q
- Laplace-LoRA
- BLoB
